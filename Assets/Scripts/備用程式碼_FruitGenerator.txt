class SpawnSystem
static

Awake
{
	RandomItem();
}
RandomItem
{
	FruitLevel[i].SetActive(true);
	Position = new Vector3(x, y, z);
}
class Item
OnTriggerEnter2D
{
	if(other.tag == "Player") // 得分 + SetActive(false)
	if(other.tag == "Ground") // SetActive(false)
	SpawnSystem.instance.RandomItem
}